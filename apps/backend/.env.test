# FounderCFO — Isolated Test Database
# This environment file is used EXCLUSIVELY for Auto-Pilot simulations.
# NEVER shares state with development or production.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/foundercfo_test?schema=public"
JWT_SECRET="test-secret-key-for-simulations-only"
JWT_EXPIRATION="1d"
NODE_ENV="test"
